feat: Implement remaining features and polish application

This commit completes the implementation of the GCP Cloud Storage Versioned File Manager by addressing the remaining tasks in the plan.

Key changes include:
- Enhanced security: Implemented robust input validation (file type and content), added security headers, and improved path traversal prevention.
- Improved logging: Introduced structured request/response logging and ensured comprehensive audit logging for all GCP operations, including IP addresses.
- Performance optimizations: Refactored file listing and folder navigation to leverage GCP Storage delimiters for more efficient metadata queries.
- Unit testing: Added comprehensive unit tests for File, FileVersion, GCPStorageService, and FileService models and logic.
- Documentation updates: Updated README.md and quickstart.md with detailed setup and usage instructions, including environment variable configuration.
- Deprecation fixes: Replaced deprecated `datetime.utcnow()` with `datetime.now(timezone.utc)` across the codebase for future compatibility.
- Dependency management: Added `python-magic` to `requirements.txt` for content-based file type detection.